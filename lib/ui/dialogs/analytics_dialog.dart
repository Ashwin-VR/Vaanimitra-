// DEV 3 — implement per PDR Section 12.4
import 'package:flutter/material.dart';
class AnalyticsDialog extends StatelessWidget {
  const AnalyticsDialog({super.key});
  @override
  Widget build(BuildContext context) => const AlertDialog(title: Text('Analytics'));
}
